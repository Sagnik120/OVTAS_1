# Core dependencies (always needed).
numpy>=1.23
scipy>=1.9
torch>=2.0
pyyaml>=6.0
tqdm>=4.65

# --- Optional extras -------------------------------------------------
# Install with: pip install -e ".[vlm]"   -> real CLIP / SigLIP encoders
#               pip install -e ".[video]" -> frame extraction from videos
#               pip install -e ".[dev]"   -> pytest / linting for development
